{
  "name": "encuesta-perfiles-tecnicos",
  "version": "1.0.0",
  "description": "Encuesta de perfiles técnicos - ejecutar en cualquier PC",
  "private": true,
  "scripts": {
    "setup": "node scripts/setup.js",
    "backend": "cd backend && node src/server.js",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
